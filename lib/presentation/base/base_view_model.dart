abstract class BaseViewModel {
  // shared variables and function which will be used through ant view model.

  void dispose();

  void init();
}

abstract class BaseViewModeInputs {}

abstract class BaseViewModeOutputs {}
